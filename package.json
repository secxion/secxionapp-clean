{
  "name": "secxion-app-root",
  "version": "1.0.0",
  "private": true,
  "description": "Root for Secxion app, containing frontend and backend.",
  "scripts": {
    "compress:images": "node scripts/compress-images.mjs",
    "deploy": "./deploy.sh",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend start\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend start",
    "start": "npm --prefix backend start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "glob": "^11.0.3",
    "react-image-file-resizer": "^0.4.8",
    "sharp": "^0.34.3",
    "tailwindcss": "^3.4.3"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
